# Sanad Pro - نظام إدارة سندات الصرف

تطبيق ويب احترافي لإدارة سندات الصرف، مبني باستخدام Node.js و Express و Prisma.

## 🚀 المميزات
- واجهة عربية كاملة
- إدارة السندات (إضافة، تعديل، حذف، طباعة)
- نظام صلاحيات (مدير، محاسب، مستخدم)
- تقارير وملخصات مالية
- سجل نشاطات (Audit Log)
- جاهز للنشر على Render/Railway

## 🛠️ التقنيات المستخدمة
- **Backend:** Node.js, Express, Prisma, SQLite (Dev) / PostgreSQL (Prod)
- **Frontend:** Vanilla JS, CSS3, HTML5
- **Auth:** JWT, Bcrypt

## ⚙️ طريقة التشغيل محلياً

1. **تثبيت المتطلبات:**
   ```bash
   npm install
   ```

2. **إعداد قاعدة البيانات:**
   ```bash
   npm run prisma:migrate
   ```

3. **إنشاء المستخدم المدير:**
   ```bash
   node seed.js
   ```
   *بيانات الدخول:*
   - المستخدم: `admin`
   - كلمة المرور: `admin123`

4. **تشغيل الخادم:**
   ```bash
   npm start
   ```
   أو للتطوير:
   ```bash
   npm run dev
   ```

5. **فتح التطبيق:**
   اذهب إلى `http://localhost:3000`

## ☁️ طريقة النشر (Deployment)

### Render / Railway
1. ارفع المشروع على GitHub.
2. اربط المستودع بـ Render أو Railway.
3. **Environment Variables:**
   أضف المتغيرات التالية في لوحة التحكم:
   - `NODE_ENV` = `production`
   - `JWT_SECRET` = مفتاح سري قوي
   - `DATABASE_URL` = رابط قاعدة بيانات PostgreSQL (توفرها Railway/Render).
4. **Build Command:** `npm install && npm run prisma:generate`
5. **Start Command:** `npm start`
   *(ملاحظة: تأكد من تشغيل `npx prisma migrate deploy` لتطبيق الترحيلات على قاعدة البيانات الجديدة في الإنتاج).*

## 🔐 الصلاحيات
- **ADMIN:** صلاحيات كاملة + إدارة المستخدمين (عبر الـ API حالياً أو Seed).
- **ACCOUNTANT:** إضافة وتعديل السندات + طباعة.
- **USER:** عرض السندات والطباعة فقط.

---
تم التطوير بواسطة Antigravity
