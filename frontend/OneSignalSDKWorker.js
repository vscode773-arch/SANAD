importScripts('https://cdn.onesignal.com/sdks/OneSignalSDK.js');

self.addEventListener('fetch', function (event) {
    // A simple no-op fetch handler is often enough to trigger PWA installability criteria
    // logic can be added here for caching if needed
});
